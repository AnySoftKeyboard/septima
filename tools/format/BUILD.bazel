load("@aspect_rules_lint//format:defs.bzl", "format_multirun")

package(default_visibility = ["//visibility:public"])

format_multirun(
    name = "format",
    javascript = "//:prettier",
    kotlin = "//tools/lint:ktlint_sh_binary",
    markdown = "//:prettier",
    starlark = "@buildifier_prebuilt//:buildifier",
    yaml = "//:prettier",
)
